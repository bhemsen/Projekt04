from netaddr import *

class Inputhandler:

    def handleIPv4(self):
        return IPAddress(str(input('Bitte gib eine IPv4 ein:'))).ipv4()

    
    def handleIPv6(self):
        return IPAddress(str(input('Bitte gib eine IPv6 ein:'))).ipv6()


    def handleNewNetwork(self):
        networkadress = str(input('Gib hier die Netzwerkadresse ein: '))
        subnetmask = str(input('Gib hier die Subnetzmaske ein: '))
        hostCount = int(input('Gib hier die Anzahl der benötigten Hostadressen ein: '))

        data = []
        data.append(networkadress, subnetmask, hostCount)
        print(data)

    handleNewNetwork()